apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lingua-atlas
resources:
  - namespace.yaml
  - configmap.yaml
  - mysql.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml
# Copy secret.example.yaml to a secret manager or sealed secret before applying.
